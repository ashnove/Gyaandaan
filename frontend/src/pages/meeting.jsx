import React from "react"

function Profile(props)  {
	return (
		<div>
			This is Meeting Page. To be made available, after the request us processed.

      {isAllocated ? }

		</div>
	)
}

export default Profile
